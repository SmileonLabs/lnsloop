services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: loop
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD}
      POSTGRES_DB: loop
    ports: ["127.0.0.1:5432:5432"]
    volumes: ["loop-db:/var/lib/postgresql/data"]
volumes:
  loop-db:
